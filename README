# Development

grunt serve

# Deploy

grunt build
git subtree push --prefix dist origin gh-pages

To force push use the command bellow.

git push origin `git subtree split --prefix dist master`:gh-pages --force